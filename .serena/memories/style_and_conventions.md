Use minimal, incremental refactors. Preserve behavior, especially serialization compatibility and geometry/layout semantics. Python code uses type hints and small focused helpers where useful; avoid unnecessary rewrites. Keep business logic in `core/` Qt-free where practical. Respect existing permissive behaviors in `core/project_state.py` unless explicitly changing them. Do not touch `ui/drawing_canvas.py` broadly without strong characterization tests.