{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "/bin/bash -lc 'repo_root=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; cd \"$repo_root\" && uv run python3 \"$repo_root/.codex/hooks/session_start_review_context.py\"'",
            "statusMessage": "Loading repository workflow context"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/bin/bash -lc 'repo_root=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; cd \"$repo_root\" && uv run python3 \"$repo_root/.codex/hooks/user_prompt_review_context.py\"'",
            "statusMessage": "Refreshing workflow reminder"
          }
        ]
      }
    ]
  }
}
